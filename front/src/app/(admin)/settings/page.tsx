import FormSettings from '@/components/form-settings';
import { FC } from 'react';

const page: FC = () => {
  return (
    <section className="mt-10">
      <FormSettings />
    </section>
  );
};

export default page;
