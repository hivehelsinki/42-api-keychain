{
  "name": "keychain-back",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx prisma generate && nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jordane Gengo",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.1.0",
    "@slack/webhook": "^6.1.0",
    "dotenv": "^16.3.1",
    "node-cron": "^3.0.2",
    "nodemon": "^3.0.1"
  },
  "devDependencies": {
    "prisma": "^5.1.0"
  }
}
